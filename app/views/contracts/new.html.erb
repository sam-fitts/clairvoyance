<%= simple_form_for [@project, @proposal, @contract] do |f| %>
  <%= f.input :pdf, label: "Upload a PDF" %>
  <%= f.input :pdf_cache, as: :hidden %>
  <%= f.button :submit, "Submit contract", class: "btn btn-primary" %><br>
<% end %>
